# 崩壊スターレイル
## 行動値シミュレーター & 限界効用逓減計算機
### 設計ドキュメント（完成版） v3.0
作成日: 2026-05-15

---

# PART I　ダメージ計算式リファレンス

## 0. 全体構造

通常ダメージは7つの係数の積で算出される。各係数は独立した乗算枠であり、係数内部のバフ・デバフの合算ルールは枠ごとに異なる。

```
ダメージ = ダメージ基礎値
         × 会心係数
         × 与ダメージ係数
         × 防御係数
         × 属性耐性係数
         × 被ダメージ係数
         × 撃破係数
```

> ※ 攻撃キャラのレベルは80固定として実装（敵レベルのみ可変）

---

## 1. ダメージ基礎値

```
ダメージ基礎値 = 参照ステータス × 軌跡倍率 + ダメージ加算
```

| 項目 | 説明 |
|------|------|
| 参照ステータス | キャラ・スキルごとに異なる。攻撃力が大半だが、最大HP・防御力・速度を参照する場合もある |
| 軌跡倍率 | スキルに記載された倍率（例: 攻撃力の150%） |
| ダメージ加算 | 固定値の上乗せ。キャラ・光円錐・遺物ごとに計算式が異なる |

---

## 2. 会心係数

```
【期待値モード】
  会心係数 = 1 + 会心率% × 会心ダメージ%

【会心確定モード】
  会心係数 = 1 + 会心ダメージ%
```

| 項目 | 説明 |
|------|------|
| 会心率% | 実効上限は100%だが、サンデーE6等のように100%超を会心ダメージへ自動変換する効果が存在するため、入力値は100%超を許容する |
| 会心ダメージ% | 上限なし |
| 非会心時 | 持続ダメージ・弱点撃破・超撃破は会心非対応のため会心係数=1.0 |

---

## 3. 与ダメージ係数

```
与ダメージ係数 = 1 + 適用条件を満たす全ての与ダメージバフ%の合計
```

属性与ダメージ・スキル種別バフ・条件付きバフ等の区別なく、現在付与されており適用条件を満たすバフを全て合算する単一枠。

| バフ例 | 計算時の扱い |
|--------|-------------|
| 量子属性ダメバフ100%（常時） | 量子キャラの全攻撃で+100% |
| 戦闘スキル限定+100% | スキル使用時のみ加算 → 合計+200% |
| HP80%以下の敵に+X% | 条件成立時のみ加算 |
| 戦闘スキル後3ターン+54% | 持続中は加算（ケリュドラ専武の例） |

---

## 4. 防御係数

```
防御係数 = (20 + 攻撃キャラLv)
          ÷ {(20 + 敵Lv) × (1 - 防御ダウン% - 防御無視%)
             + 20 + 攻撃キャラLv}

Lv80固定の簡略式:
  防御係数 = 100 ÷ {(20 + 敵Lv) × (1 - 防御ダウン% - 防御無視%) + 100}
```

| 条件 | 防御係数 |
|------|---------|
| Lv80 vs Lv80 | 100 ÷ 200 = 0.500 |
| Lv80 vs Lv90 | 100 ÷ 210 = 0.476 |
| Lv80 vs Lv95 | 100 ÷ 215 = 0.465 |
| Lv80 vs Lv80, デバフ30%+貫通30% | 100 ÷ (40+100) = 0.714 |

| 項目 | 説明 |
|------|------|
| 防御ダウン% | デバフ。効果命中・効果抵抗の影響を受ける |
| 防御無視% | デバフではないため確定発動 |
| 合算ルール | 両者は同枠加算。合計-100%が上限（防御係数が1を超えない） |
| 特性 | 100%に近づくほど貢献度が急増する非線形枠 |

---

## 5. 属性耐性係数

```
属性耐性係数 = 1 - (敵の基礎耐性% - 耐性貫通% - 耐性デバフ%)
```

例
| 条件 | 計算結果 |
|------|---------|
| 弱点属性の敵 | 基礎耐性0% → 1.000倍 |
| 非弱点の敵 | 基礎耐性20% → 0.800倍 |
| 高耐性持ち | 基礎耐性40% → 0.600倍 |
| 耐性デバフで-10% | 1 - (-10%) = 1.100倍 |
| 貫通30%+デバフ20% | 1 - (20% - 30%) = 1.500倍 |

高耐性の敵に対しては耐性をいじる能力が数値以上の貢献度を発揮する。

---

## 6. 被ダメージ係数

```
被ダメージ係数 = 1 + 全ての被ダメージ増加%の合計
```

属性耐性係数とは別枠。属性被ダメージ増加・通常被ダメージ増加はすべて単一枠で加算される。

---

## 7. 撃破係数

```
撃破係数 = 0.9   （靭性が残っている敵への攻撃）
         = 1.0   （弱点撃破状態の敵への攻撃）
```

通常戦闘では靭性を削り切るまで常に0.9倍。弱点撃破維持で約11%のダメージ向上が得られる。

---

## 8. 派生ダメージ

### 8.1 付加ダメージ（通常）

通常ダメージと同一の計算式。「付加ダメージ」は種別フラグであり、全バフ・デバフを受ける。EPを回収できない・攻撃とみなされない点に注意。

> **例外処理：** 会心率・会心ダメージが固定される効果のような例外あり（例：ロビン追加攻撃の会心率100%・会心ダメージ150%固定）。

### 8.2 持続ダメージ（DoT）

```
持続ダメージ = 持続ダメージ基礎値
             × 与ダメージ係数
             × 防御係数
             × 属性耐性係数
             × 被ダメージ係数
             × 撃破係数
```

会心係数は適用されない。実装優先度: 中。

### 8.3 弱点撃破系ダメージ

弱点撃破ダメージ・超撃破ダメージ・弱点撃破による持続/付加ダメージ。実装優先度: 低。撃破特効が主要パラメータで、攻撃力・会心は参照しない。

### 8.4 確定ダメージ

通常ダメージ計算とは完全に独立した別個のダメージインスタンス。ダメージ計算式の係数とは別枠で発生し、敵に独立して与えられる。

```
確定ダメージ = 参照値（他のダメージの n%等、能力により変動）

最終的に敵が受けるダメージ:
  通常ダメージ計算結果 ＋ 発生した全ての確定ダメージの合計
```

| 項目 | 説明 |
|------|------|
| 無属性 | 属性の概念がない（属性耐性の影響を受けない） |
| バフ・デバフ無効 | 与ダメージバフ・被ダメージ増加等あらゆるバフ・デバフの影響を受けない |
| 防御力無効 | 敵の防御力・耐性の影響を受けない |
| 会心なし | 会心は発生しない |
| 相互非参照 | 確定ダメージ同士は計算時に互いを参照しない（連鎖しない） |
| 加算関係 | 結果として全ての確定ダメージは単純加算で合算される |

> 確定ダメージの発生条件・参照対象は能力ごとに大きく異なるため型に収めない。キャラ実装時に個別ロジックで対応する。

**例：**
- 通常ダメージのn%を即座に確定ダメージとして追加付与
- 与ダメージを蓄積し、必殺技時に確定ダメージとして放出
- 結界中の攻撃で対象に総ダメージのn%を確定ダメージで追加

---

## 9. その他の計算式

### 9.1 デバフ付与確率

```
付与確率 = 基礎確率 × (1 + 効果命中%) × (1 - 効果抵抗%) × (1 - デバフ耐性%)

※ 固定確率はこの式をバイパスし、指定確率をそのまま使用
```

### 9.2 速度・行動値

```
速度 = 基礎速度 × (1 + 速度%バフ合計) + 速度固定値加算
行動値 = 10000 ÷ 速度

速度変動時:
  既経過分は維持、残り行動値から新速度で再計算

行動値短縮:
  残り行動値 = 残り行動値 × (1 - 短縮率%)
  ※ 行動値はマイナスにならない
```

### 9.3 ステータス計算

```
表示ステータス = 基礎値 × (1 + %バフ合計) + 固定値加算

基礎攻撃力 = キャラ固有値 + 光円錐の基礎攻撃力
```

---

## 10. ステータスの限界効用逓減

同一乗算枠内のバフは加算されるため、既存値が大きいほど追加量の貢献率が下がる。別枠であれば逓減は発生しない。

### 10.1 加算枠（与ダメ・被ダメ・耐性貫通系）

```
貢献率 = 追加バフ% ÷ (1 + 追加前の枠内合計%)

例: 与ダメバフ 0% → +50%    貢献率 = 50.0%
例: 与ダメバフ 100% → +50%  貢献率 = 25.0%
```

### 10.2 会心系（合成枠）

```
会心期待値 = 1 + 会心率% × 会心ダメージ%
貢献率 = (追加後期待値 - 追加前期待値) ÷ 追加前期待値
```

### 10.3 攻撃力系（別枠）

```
貢献率 = 追加後攻撃力 ÷ 追加前攻撃力 - 1

例: 攻撃力2000 → 2200  貢献率 = 10.0%
```

### 10.4 防御系（非線形枠）

```
貢献率 = (追加後防御係数 ÷ 追加前防御係数) - 1

例（Lv80 vs Lv80）:
  防御ダウン 0% → 30%:  0.500 → 0.588 (+17.6%)
  防御ダウン 50% → 80%: 0.667 → 0.833 (+25.0%)
```

---

# PART II　アプリケーション設計

## 11. アプリ概要

### 11.1 目的

- 速度と行動値から行動回数をシミュレーションする
- ステータスの限界効用逓減を計算し、厳選・装備変更の真の価値を可視化する
- パーティ構成・ビルドを自由に設定し、火力出力も可能にする

### 11.2 技術スタック

Vanilla HTML / CSS / JavaScript（フレームワーク不要、ローカルホストで動作）

```
index.html         エントリーポイント
css/style.css      スタイル
js/
  simulator.js     行動値シミュレーター
  damage.js        ダメージ計算式
  diminishing.js   限界効用逓減
  ui.js            UI共通処理
  characters/      キャラ実装フォルダ
```

### 11.3 フェーズ計画

| フェーズ | 内容 |
|---------|------|
| Phase 0 | 最初にアプリ、UI、基本的な画面の骨組みを作る |
| Phase 1 | 行動値と速度の計算、行動回数のシミュレーターを作る |
| Phase 2 | 手動シミュレーター（戦闘UIを模した手動操作） |
| Phase 3 | 自動シミュレーション + 行動回数統計出力 |
| Phase 4 | 限界効用逓減計算機（独立タブ） |
| Phase 5 | 通常ダメージ・付加・持続の火力計算統合 |
| 将来 | 弱点撃破系・敵行動・被ダメージ計算 |

---

## 12. 戦闘シミュレーターUI

実際のゲーム戦闘画面に似せた配置を採用。モーション・アニメーションは実装しない。ボタンを押して効果が発動し、「次のターンへ」進む簡略フローとする。

### 12.1 画面構成

| エリア | 内容 |
|--------|------|
| 上部 | 行動順アイコン列（行動値ベースで動的に並び替え） |
| 中央 | 敵エリア（ダミー表示でそれっぽく） |
| 下部左 | 現在行動中キャラのアイコン + 名前 |
| 下部中央 | 行動ボタン（通常・スキル・必殺技・天賦…キャラごとに可変） |
| 下部右 | 味方アイコン列（対象選択用） |
| 右パネル | バフ・デバフ一覧（永続枠 / 都度枠で分類表示） |

### 12.2 操作フロー

```
1. 行動キャラが行動ボタンから何か行動を選択
2. 行動の効果が発動（攻撃・バフ付与・行動値短縮等）
3. ステータス・バフ・行動値が即時に更新される
4. 「次のターンへ」ボタンで次の行動キャラに進む
```

---

## 13. キャラクター実装

### 13.1 基本ステータス

| 項目 | 説明 |
|------|------|
| 基礎攻撃力 | キャラ固有値 + 光円錐の基礎攻撃力 |
| 基礎HP | キャラ固有値 + 光円錐 |
| 基礎防御力 | キャラ固有値 + 光円錐 |
| 基礎速度 | キャラ固有値 |
| 会心率・会心ダメ | 光円錐・遺物・軌跡 |
| EP最大値 | キャラごとに異なる |
| EP消費値 | 通常型: 最大=消費 / 部分消費型: 最大≠消費 / 特殊型: キャラ固有 |

> 特殊EPはキャラ実装時に個別ロジックで対応できるよう拡張性を確保。

### 13.2 スキル定義

スキルスロットは固定しない。キャラごとに自由な数・種類のスキルを定義可能。

| スキル種別 | 備考 |
|-----------|------|
| 通常攻撃 | ほぼ全キャラが持つ |
| 戦闘スキル | ほぼ全キャラが持つ |
| 必殺技 | ほぼ全キャラが持つ |
| 天賦 | キャラにより1〜複数。発動条件付きが多い |
| 追加攻撃 | 特定トリガーで発動 |
| 秘技 | 戦闘前準備等 |
| その他 | キャラ固有のカスタムスキル |

---

## 14. スキル効果の型

効果ごとに「発動条件を判定するロジックを付けるか否か」を実装時に決める。

### 14.1 攻撃系

| 種別 | 説明 |
|------|------|
| 単体攻撃 | 敵単体に倍率指定で攻撃 |
| 拡散攻撃 | 指定した敵と左右の敵。中央と左右で倍率を個別指定可能（例: 中央100%・左右50%） |
| バウンド攻撃 | 概念に複数バリエーション有り（後述）。回復スキル等の非攻撃でも使用される |
| 全体攻撃 | 全敵に倍率指定で攻撃 |
| 分担攻撃 | 総ダメージを算出後、全敵に均等分配。敵が少ないほど火力集中 |
| 追加攻撃 | 何らかのトリガーで発動する攻撃。単体・拡散・全体など内部仕様は様々 |

**バウンドの種類：**

| 種別 | 説明 |
|------|------|
| 完全ランダム型 | 全対象からランダム選択（重複あり） |
| 未ヒット優先型 | 未ヒット対象を優先しつつランダム選択（重複なし優先） |

### 14.2 回復・シールド系

| 項目 | 説明 |
|------|------|
| 対象範囲 | 単体 / 拡散 / 全体 / バウンドから選択 |
| 参照ステータス | 自由に複数選択可能（自身のHP%＋固定値、攻撃力%、防御力%等） |
| 回復タイプ | HP回復 / シールド付与 |

### 14.3 バフ付与系

| 項目 | 説明 |
|------|------|
| 対象範囲 | 単体 / 拡散 / 全体から選択 |
| 参照ステータス | 自由に複数選択可能 |
| 対象パラメータ | 攻撃力%、会心率、会心ダメ、速度、与ダメバフ等 |

### 14.4 EP回復系

| 項目 | 説明 |
|------|------|
| 対象範囲 | 単体 / 拡散 / 全体 |
| 固定値回復 | 指定値をそのまま回復 |
| 割合回復 | 回復対象キャラの最大EPに対する割合 |

### 14.5 行動値短縮

| 項目 | 説明 |
|------|------|
| 対象 | 自身 / 味方単体 / 味方全体 / 条件を満たす全ての味方 |
| 効果 | 対象の残り行動値をn%短縮 |
| 制限 | 行動値はマイナスにならない |

### 14.6 デバフ付与系

| 項目 | 説明 |
|------|------|
| 対象パラメータ | 防御ダウン、攻撃ダウン、被ダメージ増加、各種デバフ |
| 基礎確率 | 効果命中・効果抵抗・デバフ耐性を経由した付与確率計算 |
| 固定確率 | 全てバイパスし、指定確率をそのまま使用 |
| スキル設定時 | 基礎確率か固定確率のどちらを使うか選択可能 |

### 14.7 行動遅延（実装後回し）

行動値短縮の逆。ターン獲得に必要な行動値が増加する。計算式は短縮を流用可能。付与確率はない。

### 14.8 持続ダメージ起爆（実装後回し）

敵のターンが来るか、起爆効果使用時に持続ダメージを発生させる。実装は将来。

### 14.9 特殊な能力

型に収められない能力。例：会心率100%超過分を会心ダメージへ自動変換（サンデーE6）など。列挙は不可能なため、キャラ実装時にコードで直接記述する。

---

## 15. 発動条件

| 条件種別 | 説明 |
|---------|------|
| HP条件 | 自分・味方・敵のHP%が指定値以上/以下 |
| ターン条件 | n ターン目以降など |
| 状態条件 | 特定のバフ・デバフが付与されているか |
| スタック条件 | 特定スタックがn個以上 |
| 確率条件 | X% の確率で発動 |
| 無条件 | 常時発動 |

---

## 16. バフ・デバフ管理

### 16.1 反映タイミング

| 項目 | 説明 |
|------|------|
| 戦闘前反映 | ステータス画面に表示されるバフ。常時加算されているとみなす |
| 戦闘中のみ反映 | 戦闘開始後に初めて有効になるバフ |
| 設定 | 各バフごとに「戦闘前反映」フラグを設定可能 |

### 16.2 表示分類

| 分類 | 説明 |
|------|------|
| 永続枠 | 常時乗っているとみなせるバフ（ほぼ毎ターン維持できる等） |
| 都度枠 | 確実には乗らないバフ（持続ターンが短い・条件が厳しい等。例: ブローニャの加速効果） |
| 設定 | デフォルト分類はキャラ実装時に設定。ユーザーが変更可能 |

### 16.3 継続ターン

| 項目 | 説明 |
|------|------|
| 有限ターン | 数値で入力 |
| 無限 | ∞と表示 |
| +1ターン特性 | 「自キャラのターン中に付与されたバフはそのターン終了時に減らない」というゲームのおそらく共通仕様。意識すると有利なバフに専用アイコンを表示 |

> 将来的にバフのターンを厳格に自動管理する際は、各バフに「付与タイミング（誰のターン中に付与されたか）」を記録する構造が必要。

---

## 17. トリガー型バフ

通常バフ（ステータス加算）とは異なり、特定アクションに反応して別の効果を発動するバフ。トリビーの「どたばたトリビー」が代表例。

### 17.1 構造

```
トリガー型バフ = {
  付与対象,
  トリガー条件（必殺技使用・攻撃ヒット・ターン開始等）,
  消費するか否か（消費型 / 維持型）,
  発動する効果（追加攻撃・バフ付与・EP回復等）,
  発動上限（1回・n回・無制限）
}
```

17.2 トリビーの例

| 項目 | 内容 |
|------|------|
| 付与対象 | 味方全体 |
| トリガー条件 | 付与されたキャラが必殺技を使用 |
| 消費 | 消費型（発動後そのキャラのバフ消失） |
| 発動効果 | トリビーの追加攻撃「どたばたトリビー」 |
| 発動上限 | トリビーの必殺技1回につき最大3回 |

「消費されない維持型のトリガー」「ステージギミックと連動した複雑なもの」など、型では収まらないケースも存在する。そうした特殊バフはキャラ実装時にコードで直接記述する。

---

## 18. 特殊バフの実装方針

型に収められない特殊な動きをするバフ・効果については、汎用的な型に収めようとせずにキャラ実装時にコードで直接記述する方針を採用する。

### 18.1 理由

- 型に収めようとすると型の定義自体が複雑化し、結局コードと変わらない複雑さになる
- 型を定義した瞬間に「この型では表現できない例外」が必ず出てくる
- 新キャラ・新仕様の追加が制限される

### 18.2 実装方法

```
通常バフ・デバフ  → 型で管理（ステータス加算・EP回復・行動値短縮等）
トリガー型バフ    → 型で管理（構造が比較的共通）
特殊バフ          → キャラ実装時にコードで直接記述
```

特殊バフ用に「任意のコールバック関数を登録できるフック」を用意することで、型を強制せず柔軟に拡張可能とする。

---

## 19. 設計上の注意点

- ダメージ加算（固定値加算）はキャラ・スキル個別の計算式が必要
- 参照ステータスはスキル単位でフラグ管理（攻撃/HP/防御/速度）
- バフは「適用条件」と「持続情報」を持つオブジェクトとして管理
- 速度変動時は残り行動値を新速度で再計算（経過分は維持）
- 行動値短縮は任意タイミングで発動可能（1ターン中に複数回も可）
- 行動値はマイナスにならない（短縮効果がオーバーした分は無駄になる）
- 会心モード（期待値/確定）は計算時に切替可能にする
- キャラLvは80固定、敵Lvのみ可変
- バフのターンを将来的に自動管理する場合、付与タイミング記録が必要
- スキルスロットを固定しない（キャラ実装時に自由定義）
- 効果命中・効果抵抗・デバフ耐性の正確な計算式は今後の課題
- 確定ダメージは通常ダメージ計算と完全分離し、独立イベントとして処理

---