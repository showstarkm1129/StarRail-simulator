# 検証スタック

実装・編集後のデグレを防ぐ 4 層の検証スタック。**アプリ実行時はゼロ依存のまま**、
ここで使うツールは開発時のみの devDependencies。

## 4 層

| 層 | 目的 | コマンド | 中身 |
|---|---|---|---|
| 文法・型チェック | 型の破綻・存在しない定数参照・import 解決失敗を検出 | `npm run check` | TypeScript `tsc --checkJs` (JSDoc/推論。出力なし) |
| Lint (ルール・見た目) | 未定義参照・未使用・`var`・等価演算子などの規約違反 | `npm run lint` | ESLint flat config |
| テスト (回帰ゲート) | 計算ロジックの振る舞いを固定し、変更時のデグレを検出 | `npm run test` | Node 組込み `node --test` (依存ゼロ) |
| カバレッジ (改悪防止) | テストが守る範囲が痩せていないかを閾値で監視 | `npm run coverage` | `node --test` の組込みカバレッジ + 閾値 |

```bash
npm run verify   # check → lint → coverage を一括実行 (CI / コミット前ゲート)
npm run lint:fix # 自動修正可能な Lint 指摘を修正
```

## 設計判断

- **型チェックの対象は計算ロジック層** (`js/build` / `js/data`)。
  ここは型でデグレを防ぐ価値が高く、`STAT.DMG_QUANTUM` のような存在しない枠キー参照を検出できる
  (実際にこの仕組みで Cipher / Aventurine の枠キー誤りを検出・修正済み)。
  DOM グルー層 (`js/ui` / `js/bootstrap` / 非モジュール `js/*.js`) は `Element` narrowing 由来の
  ノイズが大きいため型チェック対象外とし、**構文は ESLint で担保**する。
- **シミュレータ注入グローバル** (`window.applyMark` 等) は [`types/globals.d.ts`](types/globals.d.ts) で型宣言。
- **テストは実データに依存しない** — [`test/fixtures.js`](test/fixtures.js) の決定的フィクスチャを Registry に登録して計算を検証する。
  乱数は差し替え可能 (`rng`) なので再現性あり。
- **スモークテスト** ([`test/dataLoad.test.js`](test/dataLoad.test.js)) は本物の全 `_index.js` を import し、
  データ追加時の構文/登録エラーを 1 ファイル単位で検出する回帰ゲート。
- **カバレッジ閾値はラチェット**。現状値のわずかに下に設定 (`lines 90 / funcs 92 / branch 65`、計測値は ~96 / ~98 / ~69)。
  テストを増やしたら閾値も引き上げて「痩せ」を防ぐ。`package.json` の `coverage` スクリプトで調整。

## ファイル

| パス | 役割 |
|---|---|
| `tsconfig.json` | 型チェック設定 (`noEmit` — 1 ファイルも出力しない) |
| `eslint.config.js` | Lint 設定 (ES Modules / Node / 非モジュールの 3 群を区別) |
| `types/globals.d.ts` | シミュレータ注入グローバルの型宣言 |
| `test/fixtures.js` | テスト用の決定的フィクスチャ (キャラ/セット/ビルド) |
| `test/*.test.js` | 各モジュールのテスト (registry / statKeys / skillUtil / substatRoller / statComputer / diminishing / dataLoad) |

## 新規実装後にやること

1. 該当ロジックのテストを `test/<module>.test.js` に追加 (フィクスチャを使い決定的に)。
2. `npm run verify` が緑であることを確認。
3. カバレッジに余裕が出たら `package.json` の閾値を引き上げる。
