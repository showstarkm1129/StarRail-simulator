# AIアシスタントの機能要件メモ

## 目的

AIは外部サイトを広く検索して候補を集めるのではなく、シミュレーター内で整備・検証済みの情報を利用する。利用者が作成・保存したビルドも利用し、個人の環境へすぐ適応できるようにする。

## 共通要件

- AI画面では、今回扱う対象を「限界効用逓減」または「行動順」から明示的に1つ選ぶこと。AIへは選択中の対象に対応する情報と計算機能だけを公開する。
- AIは、ユーザーが指示した対象とUI上で選択された対象を最優先で利用できること。
- 比較依頼を満たすために必要な場合だけ、保存済みの別ビルドなど追加のサイト内データを参照できること。関係のないデータは読み込まないこと。
- AIは、現在の条件に関係するキャラクター、光円錐、遺物、オーナメント、効果を利用できること。
- AIは、サイト内の信頼できる情報を基に比較・提案・説明できること。
- ユーザーが作成したビルドを読み込むことで、そのユーザー固有の環境に適応できること。
- 限られたサイト内情報から候補を扱えること。外部サイトから候補を一つずつ探すことを前提にしない。
- 比較に必要な情報が不足している場合、AIは不足項目を明示して先に質問すること。推測で計算を進めないこと。
- 1つの依頼内で、複数のビルド・装備・速度・パーティー条件などを比較できること。
- 入力を固定するプロンプトテンプレートは置かず、利用者が自由に依頼を書けること。
- 限界効用逓減では最大4人の保存ビルドを選択できること。行動順では保存済み行動順JSONを選択でき、同時に最大4人の保存ビルドも速度参照用として選択できること。

## 行動順シミュレーターの要件

- 現在のパネル状態と、読み込んだJSONをAIが利用できること。
- 保存済みキャラビルドをAIが利用できること。
- 現在の状況で使用できる、行動順に影響する手段をAIが利用できること。
- これらの情報を基に、行動順の比較・検証を行えること。

## 限界効用逓減の要件

- 現在の比較状態をAIが利用できること。
- 複数の保存済みビルドと、光円錐などの候補を取得して比較できること。
- パーティーを含む利用者固有の条件を比較へ反映できること。

## 変更案の要件

- AIは、比較結果に基づく変更案を出力できること。
- 利用者が確認した変更案は、画面上の状態へそのまま反映できること。
